apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-backend.fullname" . }}
  labels:
    {{- include "hello-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
  selector:
    app: {{ include "hello-backend.name" . }}
